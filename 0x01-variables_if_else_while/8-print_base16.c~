#include <stdio.h>

/**
 *main - Entry point
 *Description: Program that prints all the hexadecimal numbers
 *Can only use te putchar function
 *Can only use putchar three times in code
 *Return: 0
 */

int main(void)
{
int n;
for (n = 48; n < 58; n++)
{
putchar(n);
}
for (n = 97; n <= 102; n++)
{
putchar(a);
}
putchar('\n');
return (0);
}
