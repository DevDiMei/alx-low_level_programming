#include<stdio.h>

/**
 *printf - sends formatted string to the output
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art"/n);
return (0);
}
